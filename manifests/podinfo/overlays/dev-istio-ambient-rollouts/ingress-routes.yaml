# Gateway API HTTPRoutes for podinfo external access (ambient mode)
# These attach to the podinfo-gateway and route external traffic to backend services
---
# Stable version direct access
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo-stable-route
  namespace: podinfo-ambient
spec:
  parentRefs:
  - name: podinfo-public-gateway
    group: gateway.networking.k8s.io
    kind: Gateway
    namespace: istio-system
  hostnames:
  - stable-podinfo-ambient-rollouts.drozd.cloud
  - stable-podinfo-ambient-rollouts.local
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: podinfo-stable
      group: ""
      kind: Service
      port: 80
      weight: 1
---
# Canary version direct access
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo-canary-route
  namespace: podinfo-ambient
spec:
  parentRefs:
  - name: podinfo-public-gateway
    group: gateway.networking.k8s.io
    kind: Gateway
    namespace: istio-system
  hostnames:
  - canary-podinfo-ambient-rollouts.drozd.cloud
  - canary-podinfo-ambient-rollouts.local
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: podinfo-canary
      group: ""
      kind: Service
      port: 80
      weight: 1
---
# Traffic-split access (uses root podinfo service, waypoint applies HTTPRoute weights)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo-split-route
  namespace: podinfo-ambient
spec:
  parentRefs:
  - name: podinfo-public-gateway
    group: gateway.networking.k8s.io
    kind: Gateway
    namespace: istio-system
  hostnames:
  - podinfo-ambient-rollouts.drozd.cloud
  - podinfo-ambient-rollouts.local
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: podinfo
      group: ""
      kind: Service
      port: 80
      weight: 1
